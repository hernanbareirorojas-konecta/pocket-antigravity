# One substring per line, case-insensitive.
# If a run_command execution matches any of these outside quotes,
# its output is condensed to failures, tracebacks, errors, and a count summary
# instead of dumping hundreds of verbose passing lines into the LLM context.
pytest
python -m pytest
python -m unittest
npm test
npm run test
yarn test
pnpm test
bun test
deno test
jest
npx jest
vitest
npx vitest
mocha
cargo test
go test
dart test
flutter test
dotnet test
swift test
mix test
rspec
bundle exec rspec
phpunit
./vendor/bin/phpunit
gradle test
gradlew test
./gradlew test
mvn test
mvnw test
./mvnw test
ctest
