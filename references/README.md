# References Directory

Store cookbooks, benchmark logs, and domain references here.

## Usage Rule
These documents must **NEVER** be loaded automatically at session start.
The agent should only view specific reference files when explicitly instructed or when debugging a deep implementation detail.
